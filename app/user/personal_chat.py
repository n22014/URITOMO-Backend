# daiki

import models.room as room 



async def create_personal_chat(user_id: str, target_user_id: str):
    
    room = room.Room(
        id=str(uuid.自動
        name="押したあいての名前＆meのchat ROOM",
        creator_id=user_id,
        type="personal",
        created_at=datetime.utcnow(),
        updated_at=datetime.utcnow(),
    )

    